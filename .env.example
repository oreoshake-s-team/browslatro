# Copy to .env and fill in — .env* is gitignored.
# Used by scripts/generateShopTeacherDataset.ts, scripts/labelDisagreements.ts,
# and scripts/distillPolicy.ts to call the Claude teacher model.
ANTHROPIC_API_KEY=
